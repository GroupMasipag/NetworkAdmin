.gitignore = ignores library from vscode

app.py = mostly includes python for flask application - this runs on the server ( render )

Static contains css, jss and images which is mostly design / FRONTEND / BACKEND

template contains the html

requirements.txt are python dependencies

Procfile just tells render how to run the code / app 
